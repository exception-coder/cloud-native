spring:
  application:
    name: gateway
  redis:
    host: localhost
    password:
management:
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: gateway